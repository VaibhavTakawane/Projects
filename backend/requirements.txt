asgiref==3.5.2
setuptools<81  # keeps pkg_resources available; djangorestframework-simplejwt 5.2.2 needs it
Django==4.1.3
django-cors-headers==3.13.0
djangorestframework==3.14.0
djangorestframework-simplejwt==5.2.2
gunicorn==20.1.0
Pillow>=10.4.0
PyJWT==2.1.0
python-dotenv==1.0.1
pytz==2021.1
sqlparse==0.4.1
tzdata==2022.6
whitenoise==5.3.0

python-dotenv==1.0.1

# psycopg2-binary is only needed if you connect to PostgreSQL.
# This project uses SQLite locally, so it's commented out to avoid
# a build failure (it needs PostgreSQL installed to compile).
# Uncomment if you deploy with a Postgres database:
# psycopg2-binary==2.9.6